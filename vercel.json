{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install --no-package-lock --prefer-online --no-audit --no-fund",
  "buildCommand": "npm run build",
  "crons": [
    {
      "path": "/api/cron/legal-ingestion",
      "schedule": "17 1 * * *"
    }
  ]
}
